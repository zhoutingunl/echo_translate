Flask>=3.0,<4.0
flask-sock>=0.7.0
requests>=2.31
python-dotenv>=1.0
# Bailian / DashScope cloud ASR (browser fallback for Safari/Firefox).
# Only needed when DASHSCOPE_API_KEY is set; imported lazily otherwise.
dashscope>=1.20
# --- dev / test ---
pytest>=8.0
pytest-cov>=4.1
